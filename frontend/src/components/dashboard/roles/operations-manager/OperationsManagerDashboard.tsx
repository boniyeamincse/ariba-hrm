import { Dashboard } from '../../Dashboard'

// Operations Manager: Operations Dashboard & Facility Flow
// Scope: Queue SLA management, facility flow optimization, throughput
// Widgets: queue_waiting, lab_pending, pharmacy_queue, open_tasks
// Quick Actions: OPD Queue, Vitals Entry, Task Board

export function OperationsManagerDashboard() {
  return <Dashboard />
}
