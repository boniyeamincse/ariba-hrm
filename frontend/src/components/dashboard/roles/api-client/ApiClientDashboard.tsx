import { Dashboard } from '../../Dashboard'

// API Client: Third-party Integrations Sandbox
// Scope: API reliability, delivery success, integration health
// Widgets: api_calls_today, webhook_failures, active_sessions, service_health
// Quick Actions: API Keys, Webhooks

export function ApiClientDashboard() {
  return <Dashboard />
}
