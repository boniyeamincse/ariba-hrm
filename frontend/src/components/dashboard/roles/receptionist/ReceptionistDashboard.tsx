import { Dashboard } from '../../Dashboard'

// Receptionist: Front Desk, OPD Queue & Appointments
// Scope: Front desk throughput, appointment flow, patient registration
// Widgets: today_registrations, appointments_today, queue_waiting, invoices_today
// Quick Actions: Register Patient, Appointments, Billing

export function ReceptionistDashboard() {
  return <Dashboard />
}
